require 'stockpicker'

describe "stockpicker" do
  let(:days){[44, 30, 24, 32, 35, 30, 40, 38, 15]}
  it 'returns most profitable days for buying then selling' do
    expect
  end
  it 'throws an error if input is not an array'
  it 'throws an error if any inputs in array are not a number'
  it 'throws an error if '


end
